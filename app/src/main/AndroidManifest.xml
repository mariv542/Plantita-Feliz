<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- GPS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--  notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!--  camara -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false"/>

    <!--  Guardar fotos  -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HappyPlant">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.happyplant.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>


        <!-- Activity principal -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- otras actividades -->
        <activity android:name=".view.login_activity" />
        <activity android:name=".view.menu_activity" />
        <activity android:name=".view.perfil_activity" />
        <activity android:name=".view.ecoAvance_activity" />
        <activity android:name=".view.ecoAviso_activity" />
        <activity android:name=".view.ecoControl_activity" />
        <activity android:name=".view.ecoPlanta_activity" />
        <activity android:name=".view.registrar_activity" />
        <activity android:name=".view.ecoUsuario_activity" />
        <activity android:name=".view.dispositivos_activity" />
        <activity android:name=".view.soporteActivity" />
    </application>

</manifest>